'use client';
import {Card,Controls,Empty,Shell,useFilters,options,matches,type Prompt} from '../shared';
export function Atlas({rows}:{rows:Prompt[]}){const f=useFilters(rows),tasks=options(f.found,'useCases');return <Shell rows={rows} note="Start with what you want to make. Each task is a column of images; choose a model to compare its range."><Controls rows={rows} f={f}/><div className="results-heading"><h2>A visual index, by task</h2><span role="status">{f.found.length} of {rows.length} prompts</span></div><p className="matrix-note">A prompt can appear in more than one task. Swipe across to explore every column.</p>{f.found.length?<div className="atlas" tabIndex={0} role="region" aria-label="Task columns, scroll horizontally">{tasks.map((t,i)=><section className="lane" key={t.slug}><header className="lane-heading"><span className="micro">{String(i+1).padStart(2,'0')}</span><h2>{t.label}</h2><span className="badge">{t.count}</span></header>{f.found.filter(p=>matches(p,'useCases',t.slug)).map(p=><Card p={p} key={p.id}/>)}</section>)}</div>:<Empty clear={f.clear}/>}</Shell>}
