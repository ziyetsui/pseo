import { Argument } from '../../credit-shared';
/* Baseline: the first screen exactly as it ships today — no credit. Flip to it to check that the
   paragraphs above never move. */
export default function Current() { return <Argument />; }
