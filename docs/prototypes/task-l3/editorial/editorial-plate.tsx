'use client';
import {Shell,Picture,Actions,Words,number,type EditorialProps} from './shared';
export default function Plate(props:EditorialProps){return <Shell {...props} direction="plate" title={<>{props.task.label},<br/>one prompt at a time.</>} intro="Each result reproduced whole, with its caption in the margin: the model, the author, and the words behind it.">{rows=><><section className="ed-contents ed-wrap"><div className="ed-section-head"><h2>List of plates</h2><span className="ed-label">{rows.length} works in this selection</span></div><nav className="ed-plate-index" aria-label="List of plates">{rows.map((p,i)=><a key={p.id} href={`#plate-${p.id}`}><span>{number(i)}</span><span>{p.title}</span></a>)}</nav></section><section className="ed-wrap" aria-label="Plates">{rows.map((p,i)=><article className={`ed-plate-row${i%2?' ed-verso':''}`} key={p.id} id={`plate-${p.id}`}><Picture prompt={p} multiple/><div className="ed-caption"><p className="ed-label ed-rule">Plate {number(i)}</p><h2>{p.title}</h2><dl><dt>Model</dt><dd>{p.models.map(m=>m.label).join(' / ')||'Not named'}</dd><dt>Medium</dt><dd>{p.kind}</dd><dt>Credit</dt><dd>{p.handle||'Unattributed'}</dd><dt>Style</dt><dd>{p.styles.map(s=>s.label).join(' / ')||'Not named'}</dd></dl><p className="ed-excerpt">{p.prompt.replace(/\s+/g,' ').slice(0,320)}{p.prompt.length>320?'…':''}</p><Actions prompt={p}/><Words prompt={p}/></div></article>)}</section></>}</Shell>}
