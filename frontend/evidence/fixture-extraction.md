# Fixture extraction record

GENERATED by `scripts/extract-wireframe.mjs` (`pnpm extract:wireframe`) — do not edit by hand.

- Source: `docs/wireframes/flow-proto.html`
- Snapshot (`observedAt`): **2026-08-20** — the wireframe's own `CUT` table and footer date.
- Trending windows: 7d ≥ 2026-08-13, 30d ≥ 2026-07-21.
- Content hash of this run: `3211f8083ff6ffc6`

## 1. Cards per page

| Page | `article.card` nodes | unique X status ids |
| --- | --- | --- |
| L1 (`template#allcards`) | 21 | 21 |
| L2 (image gallery) | 18 | 13 |
| L3 (Nano Banana Pro) | 14 | 13 |
| L4 (golden record) | 1 | 1 |
| **merged** | 54 | **35** |

L2 renders 18 card nodes but only 13 distinct prompts: the 精选 rail and the per-model rails re-use the same cards. Same for L3 (14 nodes / 13 prompts).

Cross-page overlap (unique ids):

- L1 ∩ L2: 7
- L1 ∩ L3: 5
- L2 ∩ L3: 6
- L4 is disjoint from L1/L2/L3 (2063814043631280180 appears on no other page).

Merge rule: identity = X status id. Taxonomies are unioned across pages, the longest prompt text and the richest media list win, `appearsOn` records every page the prompt was seen on.

## 2. Prompt records

- Total: **35** (expected 35).
- Slug source: `curated` 1 · `derived` 14 · `wireframe-slug` 20 — `wireframe-slug` values are copied verbatim from the L2/L3 `详情 →` hrefs; `derived` follows the prototype's own rule `slugify(title).slice(0,44) + "-" + id` (verified to reproduce all 32 published slugs exactly).
- With a publish date: 22 / 35. L2/L3 cards carry no date, so a prompt that appears only there has `publishedAt: null` (never 0, never a guess) and is excluded from every trending window.
- With a `valueScore`: 21 / 35 (only L1 exposes `data-vs`).
- `highValue`: 30 / 35 (L1 `data-hv="1"` or the L2/L3 `热门` badge).
- Metrics rounded from an abbreviated `3.8K`-style label: 0 (flagged with `metricsRounded: true`). Exact L1 values win whenever the same prompt also appears on L1.
- `views/reposts/replies/quotes`: only the L4 golden record publishes them; every other prompt stores `null`.

Content type:

- `image`: 23
- `unknown`: 1
- `video`: 11

Rule: `video` when the media badge says 视频/VIDEO, `image` when it says 图片/PHOTO or the prompt appears on L2/L3, otherwise `unknown`.

Slug rule check: the derived rule reproduces **all 20** slugs the prototype publishes on L2/L3 — the extractor asserts on any mismatch.

## 3. Taxonomy

### model (11 terms)

| slug | canonical label | aliases kept | prompts in fixture | wireframe declared |
| --- | --- | --- | --- | --- |
| `gpt-image` | GPT Image | — | 4 | 115 |
| `gpt-image-2` | GPT Image 2 | — | 4 | 50 |
| `higgsfield-soul` | Higgsfield Soul | — | 4 | 118 |
| `kling` | Kling | — | 1 | 76 |
| `nano-banana` | Nano Banana | — | 5 | 218 |
| `nano-banana-2` | Nano Banana 2 | — | 3 | 40 |
| `nano-banana-pro` | Nano Banana Pro | — | 14 | 136 |
| `seedance` | Seedance | — | 11 | 272 |
| `sora` | Sora | — | 0 | 13 |
| `veo` | Veo | — | 1 | 26 |
| `wan` | Wan | — | 0 | 11 |

### useCase (8 terms)

| slug | canonical label | aliases kept | prompts in fixture | wireframe declared |
| --- | --- | --- | --- | --- |
| `advertising` | Advertising | — | 5 | 133 |
| `automotive` | Automotive | — | 1 | 68 |
| `beauty` | Beauty | — | 13 | 149 |
| `fashion` | Fashion | — | 5 | 162 |
| `food-beverage` | Food & beverage | — | 8 | 81 |
| `product-marketing` | Product marketing | — | 0 | 45 |
| `ugc` | UGC | — | 2 | 17 |
| `web-motion-design` | Web & motion design | `Web & motion` | 3 | 25 |

### technique (8 terms)

| slug | canonical label | aliases kept | prompts in fixture | wireframe declared |
| --- | --- | --- | --- | --- |
| `camera-movement-shot-language` | Camera movement / shot language | `Camera / shot language`, `Camera movement` | 29 | 698 |
| `character-consistency` | Character consistency | — | 0 | 41 |
| `image-to-video` | Image-to-video | — | 0 | 69 |
| `lip-sync-dialogue` | Lip sync / dialogue | — | 4 | 75 |
| `miniature-photography` | 微缩摄影 | — | 1 | — |
| `multi-shot-storyboard` | Multi-shot / storyboard | — | 4 | 174 |
| `text-to-video` | Text-to-video | — | 0 | 8 |
| `transition-morph-match-cut` | Transition / morph / match cut | `Transition / morph` | 4 | 110 |

### style (7 terms)

| slug | canonical label | aliases kept | prompts in fixture | wireframe declared |
| --- | --- | --- | --- | --- |
| `anime-illustrated` | Anime / illustrated | — | 3 | 126 |
| `cinematic` | Cinematic | — | 14 | 416 |
| `luxury` | Luxury | — | 11 | 179 |
| `photorealistic` | Photorealistic | `超写实` | 27 | 393 |
| `retro-vintage` | Retro / vintage | — | 6 | 180 |
| `sci-fi-cyberpunk` | Sci-fi / cyberpunk | — | 1 | 117 |
| `surreal-fantasy` | Surreal / fantasy | — | 0 | 85 |

### subject (5 terms)

| slug | canonical label | aliases kept | prompts in fixture | wireframe declared |
| --- | --- | --- | --- | --- |
| `architecture-interior` | Architecture / interior | — | 7 | 33 |
| `food-drink` | Food / drink | — | 2 | 9 |
| `person-portrait` | Person / portrait | — | 15 | 215 |
| `product` | Product | — | 1 | 12 |
| `vehicle` | Vehicle | — | 0 | 9 |

### contentType (3 terms)

| slug | canonical label | aliases kept | prompts in fixture | wireframe declared |
| --- | --- | --- | --- | --- |
| `image` | 图片 | — | 23 | 324 |
| `unknown` | 未标注类型 | — | 1 | — |
| `video` | 视频 | — | 11 | 479 |

Canonicalisation: one term per axis, canonical label taken from the L1 tile when one exists. `wireframeDeclaredCount` is prototype metadata only — it is never rendered as an achieved count (global constraint 3).

Declared-count scopes differ between pages and are **not** comparable: L1 tiles declare library-wide counts (982-scope), L2 chips declare image-gallery counts (324-scope), L3 chips declare model-scope counts. The first (widest) one seen is kept.

## 4. Creators

- In the fixture: **21** (every distinct handle that owns at least one prompt).
- With an avatar URL: 3 (only the L3 inline list publishes avatars).
- With a follower count: 1 (only the L4 source panel publishes one).

## 5. Collections

| slug | title | rule | members in fixture |
| --- | --- | --- | --- |
| `cinematic-camera` | 电影感镜头合集 | technique=camera-movement-shot-language AND style=cinematic | 12 |
| `multi-shot-storyboard` | 多镜头分镜合集 | technique=multi-shot-storyboard | 4 |
| `transitions` | 转场特效合集 | technique=transition-morph-match-cut | 4 |
| `beauty-photorealistic` | 美妆写实合集 | useCase=beauty AND style=photorealistic | 12 |
| `template-prompts` | 模板提示词合集 | regex `\[[A-Z][A-Z0-9 _/-]{1,40}\]|@img\d+|@image\d+` | 5 |
| `advertising` | 广告创意合集 | useCase=advertising | 5 |

The prototype defines these six as JS predicates in the L1 script; they are re-expressed as serialisable rules over canonical slugs (so `Camera movement` → `camera-movement-shot-language`, `Transition / morph` → `transition-morph-match-cut`). Member counts are computed from the fixture at read time, never stored.

## 6. Models

| slug | label | prompts in fixture | prototype page | declared count |
| --- | --- | --- | --- | --- |
| `gpt-image` | GPT Image | 4 | no (`#`) | 115 |
| `gpt-image-2` | GPT Image 2 | 4 | yes | 50 |
| `higgsfield-soul` | Higgsfield Soul | 4 | yes | 118 |
| `kling` | Kling | 1 | no (`#`) | 76 |
| `nano-banana` | Nano Banana | 5 | no (`#`) | 218 |
| `nano-banana-2` | Nano Banana 2 | 3 | no (`#`) | 40 |
| `nano-banana-pro` | Nano Banana Pro | 14 | yes | 136 |
| `seedance` | Seedance | 11 | no (`#`) | 272 |
| `sora` | Sora | 0 | no (`#`) | 13 |
| `veo` | Veo | 1 | no (`#`) | 26 |
| `wan` | Wan | 0 | no (`#`) | 11 |

`ModelDetail` is generated for every model with ≥ 1 prompt (9 of 11); its summary, capabilities, inputs, outputs and editorial numbers are all computed from the fixture, never copied from the prototype's 157/68/136 figures.

## 7. L4 golden record

- id `2063814043631280180`, slug `country-miniature-stamp-poster` (curated, per the task brief).
- `[COUNTRY]` occurs **7** times — counted by the parser, not copied from the prototype's "7 处" copy.
- 6 country options, 4 usage steps, 4 tail parameters, 6 variations (all `status: "pending"`, `media: null` — the prototype renders 待生成 placeholders).
- 4 media items, followers 34683, views 7318 / likes 185 / bookmarks 122 / reposts 20 / replies 44 / quotes 2.

## 8. Excluded or transformed data

| datum | reason |
| --- | --- |
| contentType `未标注类型`（slug `unknown`） | 原型没有这个词条：为原帖无媒体、且不在图片页面的 Prompt 自造的诚实兜底标签，不是原型数据 |
| L1 card 2019109812341207229 media | 原帖无媒体（prototype placeholder "原帖无媒体"）→ media: [] |
| L2 content-type tile "mixed"（声明 84 条） | 未建模的内容类型：Prompt 只区分 image / video / unknown |
| L2 content-type tile "unresolved"（声明 91 条） | 未建模的内容类型：Prompt 只区分 image / video / unknown |
| L2 content-type tile "网页"（声明 4 条） | 未建模的内容类型：Prompt 只区分 image / video / unknown |
| L2 model tile "GPT Image" href | 原型为 `#` 占位链接：本阶段不渲染为链接 |
| L2 model tile "Kling" href | 原型为 `#` 占位链接：本阶段不渲染为链接 |
| L2 model tile "Nano Banana 2" href | 原型为 `#` 占位链接：本阶段不渲染为链接 |
| L2 model tile "Nano Banana" href | 原型为 `#` 占位链接：本阶段不渲染为链接 |
| L2 model tile "Seedance" href | 原型为 `#` 占位链接：本阶段不渲染为链接 |
| L4 chip `Higgsfield` | 生成平台标签（brief 明确不建模为 taxonomy） |
| L4 chip `Prompt` | 内容类型徽标，不是 taxonomy 词条 |
| 创作者 @CHAO2U_AI（原型声明 31 条） | 四页中没有任何一张卡片属于该创作者，收录会渲染出 0 条的空创作者 |
| 创作者 @Gdgtify（原型声明 4 条） | 四页中没有任何一张卡片属于该创作者，收录会渲染出 0 条的空创作者 |
| 创作者 @ManuAGI01（原型声明 35 条） | 四页中没有任何一张卡片属于该创作者，收录会渲染出 0 条的空创作者 |
| 创作者 @PrometheanAIX（原型声明 155 条） | 四页中没有任何一张卡片属于该创作者，收录会渲染出 0 条的空创作者 |
| 创作者 @Samann_ai（原型声明 3 条） | 四页中没有任何一张卡片属于该创作者，收录会渲染出 0 条的空创作者 |
| 模型 Sora（原型声明 13 条） | fixture 中没有任何 Prompt 点名该模型：不生成模型页、taxonomy count 为 0 |
| 模型 Wan（原型声明 11 条） | fixture 中没有任何 Prompt 点名该模型：不生成模型页、taxonomy count 为 0 |

### Systematic transformations

| datum | treatment |
| --- | --- |
| 原型声明数量（982 / 324 / 136 / 162 / 698 …） | 只作为 `wireframeDeclaredCount` / `wireframeDeclared*` 元数据保存，不进渲染路径 |
| `href="#"` 占位链接（footer 全部条目、L1 feature tiles、L4 crumb/related/nav） | 不建模为链接；Taxonomy.href 只对本阶段真实存在的页面非 null |
| `/prompts/video`、`/prompts/use-cases/*`、`/prompts/styles`、`/prompts/creators`、`/prompts/models` | 本阶段无对应页面 → 相关 Taxonomy.href = null，由页面渲染为非链接文本 |
| L2 `其他类型` tiles `unresolved` / `mixed` / `网页` | 不建模：ContentType 只有 image / video / unknown |
| 媒体尺寸 | 原型未提供 → 固定 640×360 占位并标记 `dimensionsSource: "assumed"` |
| 媒体 alt | L2/L3 的英文 alt（`photo from the source post`）统一改写为与 L1/L4 一致的中文 alt |
| L1 `data-q` 搜索索引 | 不保存：由 `fixture-repository.ts` 用 `query.ts` 的 `buildPromptSearchText()` 从 title / **完整** promptText（非 240 字的 `promptPreview`）/ handle / taxonomy label·slug·labelZh 生成，存为 `PromptSummary.searchText` |
| L3 genbox 的 设置 / 参考图 / 生成 按钮 | 无行为 → 不建模为数据（页面层按 global constraint 12 处理） |
| L3 `带变量的提示词` 标题声明 3 条，实际渲染 2 张卡 | 以实际卡片为准；`hasVariables` 由 `extractVariables()` 动态判定 |
| L1/L2/L3 的 copy / expand / tab 交互脚本 | 行为规格，不是数据 |

### Metric precision

L2/L3 render likes/bookmarks abbreviated (`3.8K`, `2.4K`, `1.2K`). Where the same prompt also appears on L1 the exact value (`3,849`) is kept; where it does not, the abbreviated value is expanded (`3.8K` → `3800`) and the record is flagged `metricsRounded: true` so a renderer can qualify it. In this run every abbreviated card also appears on L1, so 0 record(s) are flagged. No metric is ever invented: everything unavailable is `null`.

### Cross-page model-tag unions

The prototype tags the same X status id with a different model on different pages (e.g. id `2008952931484098637` is `Nano Banana` on L1 but `Nano Banana Pro` on L2/L3). Per "union taxonomies across pages", the merged record keeps every model slug it was ever tagged with, so this one prompt is a member of two model pages rather than being forced onto one. **13** of 35 merged prompts carry 2+ model slugs for this reason.

### Rules that differ from the prototype's own code

| prototype | fixture | why |
| --- | --- | --- |
| 合集「模板提示词」谓词 `/\[[A-Z][A-Z _\/]{2,40}\]|@img\d|@image\d/` | `\[[A-Z][A-Z0-9 _/-]{1,40}\]|@img\d+|@image\d+` | 与 `extractVariables()` 用同一套 token 规则，避免「合集里有、详情页说没有变量」的自相矛盾；也覆盖 `[SHOT 2]` / `[CTA-TEXT]` 这类含数字与连字符的 token |
| 热门补位：`该时段收录较少，已补充全部时段热门。` | `该时段收录较少，已补充全部时段的高分提示词。` | 同一行为，措辞点明补位来源 |
| L4 文案「全文出现 7 次」 | 由 `extractVariables()` 数出（本次为 7） | 数量必须来自当前数据 |
| L1 卡片 `data-q` 预拼检索串 | `fixture-repository.ts` 用 `query.ts` 的 `buildPromptSearchText()` 预先算好并存为 `PromptSummary.searchText`（title + **完整** promptText + handle + taxonomy label/slug/labelZh），`applyPromptQuery()` 只读这个字段，不再退化到 240 字的 `promptPreview` | 服务端与客户端共用同一构造函数，命中中文标签，且不受预览截断影响 |

### Unknown-parameter reporting

`parsePromptQuery()` reports unrecognised **keys** (`sort`, `page`, an unusable `window` value); `ContentRepository.listPrompts()` reports unrecognised **values** as `key=value` (e.g. `model=not-a-model`) and keeps the unusable chip visible so it can be removed. Nothing is dropped silently (global constraint 6).

