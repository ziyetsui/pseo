# L4 placeholder choices — 2026-09-04

All RecipeText placeholder controls now use native radio choices with a Custom option. Initial render leaves the original template untouched; choosing fills every occurrence. Custom values survive switching to a suggestion and back; Reset clears choices and custom state. Inputs remain hydration-gated and labeled; native keyboard radio navigation is preserved. Suggestions come from a frontend helper, not CMS or source-author assertions. Existing canonical options are all retained, including lists longer than five; generated suggestions are limited to five with the original default prioritized. Unknown tokens without a semantic entry retain known values and Custom. All current fixture variables have meaningful suggestions, including originally empty defaults.

Files: src/components/RecipeText.tsx, src/lib/catalog/placeholder-choices.ts, styles/recipe-choices.css; tests/placeholder-choices.test.ts, recipe.test.tsx, e2e/placeholder-choices.spec.ts and templates.spec.ts. Rules synchronized in frontend/AGENTS.md, specs/0008-prd.md and 0009-pseo-tech-arch.md. Subagent owned the suggestion helper and coverage; parent owned UI/state and browser verification.

Validation: all unit tests passed; lint 0 errors / 13 existing img warnings; typecheck passed. Targeted desktop/mobile browser run: 6 passed (17.0s), including choices, custom restore/reset, repeated substitutions, keyboard operation, screenshot/overflow checks and axe of the placeholder form. Desktop/mobile screenshots visually inspected. Root content validate/build and 18 infra tests passed. Final build:visual passed; check:static passed for 80 HTML pages and 4,729 local links. Whole-site E2E/SEO audit not rerun.

Initial checks exposed two stale test assumptions (the portrait source resembles JSON but is not valid JSON; Reset now collapses Custom inputs). Tests now preserve source-format expectations, and an explicit valid-JSON custom-quote regression proves correct escaping. Source text was not repaired or rewritten. Axe also caught low contrast during background/text crossfade; choice state colors now switch instantly and only borders transition, with final axe passing.

Fixture revision unchanged: sha256:73d488642d1168a16137b8ec48eb567e032d2393df24c343b26720d99d993948. CMS public/mirror unchanged; production not deployed. Suggestions are UI editing aids and are not written into canonical content.
