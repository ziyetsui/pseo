{
  "name": "public-prompt-lab-template",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/catalog.mjs generate",
    "check": "node scripts/catalog.mjs check",
    "validate": "node scripts/catalog.mjs validate",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run test && npm run validate && npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
